import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.github.hieulee.slaythespireboardgame',
  appName: 'Slay the Spire Board Game',
  webDir: 'dist',
}

export default config
